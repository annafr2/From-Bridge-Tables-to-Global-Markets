# =====================================================================
# NegoPlay — Python Dependencies
# Install: pip install -r requirements.txt
# Python: 3.11+
# =====================================================================

# --- Data & ML ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
gensim>=4.3
shap>=0.43
matplotlib>=3.7      # visualizations (PCA, t-SNE, radar, bar charts)

# --- LLM Providers (Gemini = default; others for cross-validation) ---
google-genai>=1.0          # Modern Gemini SDK (used by llm_client.py)
google-generativeai>=0.8   # Legacy SDK (kept for bridge_validator.py compatibility)
anthropic>=0.25
openai>=1.30

# --- Utilities ---
python-dotenv>=1.0
pydantic>=2.0
tqdm>=4.66
tenacity>=8.2
openpyxl>=3.1      # Excel export (preprocessing.xlsx)

# --- Testing ---
pytest>=7.4
pytest-cov>=4.1

# --- Code quality ---
ruff>=0.1
